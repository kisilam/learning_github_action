# learning_github_action

1. **php_caching.yml** workflow file using the **composer.json** to demonstrate how to use the GitHub Action cache.
   The **composer.json** is used to install only PHP libraries.
